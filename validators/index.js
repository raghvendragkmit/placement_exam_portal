const userValidator = require('./user.validator');
const subjectValidator = require('./subject.validator');
const paperSetValidator = require('./paper-set.validator');
const questionAnswerValidator = require('./question-answer.validator');
const examValidator = require('./exam.validator');
module.exports = {
    userValidator,
    subjectValidator,
    paperSetValidator,
    questionAnswerValidator,
    examValidator
}
