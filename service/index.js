const userService = require('./user.service');
const subjectService = require('./subject.service');
const questionAnswerService = require('./question-answer.service');
module.exports = {
    userService,
    subjectService,
    questionAnswerService
}